@model List<CourseViewModel> 

<div class="row">
    <div class="col-md-9">
        <h1>Catalogo dei Corsi</h1>
    </div>
    <div class="col-md-3 d-flex align-items-center">
        <form class="mr-auto" method="GET"> <!-- 'GET' per avere la ricerca nell'URL degli indirizzi -->
            <div class="input-group mb-3">
                <input type="text" class="form-control" name="search" placeholder="Cerca..." aria-label="Recipient's username" aria-describedby="button-addon2">
                <div class="input-group-append">
                    <button class="btn btn-outline-info" type="submit" id="button-addon2"><i class="fas fa-search"></i></button>
                </div>
            </div>
        </form>
    </div>
</div>

@foreach (CourseViewModel course in Model)
{
    <hr>
    <div class="row course">
        <div class="col-md-1">
            <img src="@course.ImagePath" alt="town" width="50px">
        </div>
        <div class="col-md-5">
            <h2>@course.Title</h2>
            <p class="@course.Author">di @course.Author</p>
        </div>
        <div class="col-md-2 rating" title="@course.Rating">
            @for(int i=1; i<=5; i++)
            {
                if(@course.Rating >= i)
                {
                    <i class="fas fa-star"></i>
                }
                else if(@course.Rating > i-1)
                {
                    <i class="fas fa-star-half-alt"></i>
                }
                else 
                {
                    <i class="far fa-star"></i>
                }
            }            
        </div>
        <div class="col-md-2 price">
            @course.CurrentPrice
            @if(!@course.CurrentPrice.Equals(@course.FullPrice))
            {<br><s>@course.FullPrice</s>}
        </div>
        <div class="col-md-2">
            <a asp-action="Detail" asp-route-id="@course.Id" class="btn btn-outline-primary">Dettagli</a>
        </div>
    </div>
}